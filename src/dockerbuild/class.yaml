name: "hellowar"
extends: "kutter"
values:
  image: "${dockerbuild.image}"
  buildScm:
    extra: true
    value: "${project.scm.url}"
  buildOrigin: 
    extra: true
    value: "${dockerbuild.origin}"
