name: "hellowar"
extends: "kutter"
fields:
  image: "${dockerbuild.image}"
  buildScm:
    extra: true
    value: "${project.scm.url}"
  buildOrigin: 
    extra: true
    value: "${dockerbuild.origin}"
